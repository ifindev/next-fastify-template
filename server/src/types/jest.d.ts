import 'jest';

// Re-export Jest types to make them globally available
declare global {
    // The types are actually defined in the @types/jest package
    // This file just ensures TypeScript recognizes them as global
}
